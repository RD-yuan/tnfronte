/**
 * @tnfronte/html-adapter
 *
 * Framework adapter for vanilla HTML / CSS / JS projects.
 *
 * OID Injection:
 *   Uses parse5 to walk the HTML document and inject data-oid.
 *
 * Code Modification:
 *   Straightforward DOM manipulation — locate element by OID,
 *   modify attributes / styles / text, serialise back.
 */
