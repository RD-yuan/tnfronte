/**
 * @tnfronte/server
 *
 * Editor Backend — Fastify-based HTTP + WebSocket server.
 *
 * Responsibilities:
 *  - Manage user projects (CRUD, file system operations)
 *  - Accept CodeActions via WebSocket and delegate to Code Mod Engine
 *  - Spawn / manage Vite dev-server instances for user projects
 *  - Push HMR notifications back to Editor UI
 */

console.log('TNFronte server — placeholder');
